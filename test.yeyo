[
  SET <x> (0),
  SET <y> (3),
  IF (<x> neq <y>) [
    SET <z> (4),
    UNSET <z>
  ] - [
    SET <z> (5),
    UNSET <z>
  ],
  WHILE (<x> neq <y>) [
    set <x> (<x> + 1)
  ],
  SAY "Hello",
  ASK <z>
]


